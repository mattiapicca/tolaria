fastapi==0.104.0
pydantic==2.5.0
requests==2.31.0
anthropic==0.7.0

# Note: uvicorn, chromadb, sentence-transformers removed for Vercel
# Vercel uses its own runtime and we use lightweight text search instead
